# macOS
.DS_Store

# Local Claude Code state (per-checkout, not for sharing)
.claude/

# Xcode user / derived artifacts
*.xcuserstate
*.xcuserdatad/
xcuserdata/
DerivedData/
build/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# XcodeGen-generated project (regenerate with ./build.sh)
*.xcodeproj/

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved
*.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/

# CocoaPods (not used, but just in case)
Pods/

# Editor
.vscode/
.idea/
*.swp
*.swo

# Build logs (captured by build-engineer, not source)
build-logs/

# Derived / build scratch (don't track)
tmp/

# On-device model weights (bundled out-of-band, not in git)
Jot/Resources/Models/

# Local safety-net stash from the Gemma-experimentation revert.
.revert-stash/

# Local ML scratch prototype (large checkpoints, not app code)
prototypes/style-adapter/

# Design handoff references (HTML/JSX prototypes, not app source)
Jot/design_handoff_setup_wizard/
